항상 한국어로 응답하세요.

## 안전 경계

- 파괴적 DB 작업(DELETE, UPDATE, ALTER)은 실행 전 확인 필수
- 프로덕션 대량 데이터 변경은 테스트/스테이징에서 먼저 검증
- 테스트 실패 시 구현 코드를 수정하여 해결
- 테스트 데이터, 픽스처, API 계약 변경 시 승인 필수

## 작업 관습

- 요청된 범위만 수정하고, 주변 코드 정리는 별도 요청 시에만 수행
- 커밋에서 구조적 변경(리네이밍, 추출)과 동작적 변경(기능 추가/수정)을 분리

## 품질 가드레일

- 기존 코드베이스의 함수와 패턴을 검색하여 재사용 우선
- 규모에 맞는 에러 처리 적용(내부 코드와 프레임워크 보증은 신뢰)
- 안정적이고 널리 채택된 라이브러리 우선 활용(소스코드 공개 의무 없는 라이선스)
