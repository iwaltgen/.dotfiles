[hooks]
enter = '''
if [ "$(uname -s)" = "Darwin" ]; then
  # 하위 디렉토리로 바로 진입하면 훅의 PWD 가 그 하위 디렉토리라서 루트를 넘깁니다.
  "$MISE_PROJECT_ROOT/bin/ensure-superpowers-docs-link.darwin.sh" "" "$MISE_PROJECT_ROOT"
fi
'''
